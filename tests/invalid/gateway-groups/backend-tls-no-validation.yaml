# TLS to a backend that is never verified is not re-encryption.
defaults:
  parent: {name: public}
  certificate: {issuerRef: {name: ca}}
groups:
  store:
    domains: [a.example.com]
    backendTLS:
      enabled: true
      targetRefs:
        - group: ""
          kind: Service
          name: store
