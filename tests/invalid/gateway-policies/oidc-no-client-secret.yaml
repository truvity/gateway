defaults:
  oidc: {issuer: https://id.example.com}
securityPolicies:
  console:
    namespace: example
    targetRefs: [{name: console}]
    oidc:
      clientID: console
      hostname: console.example.com
